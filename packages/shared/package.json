{
  "name": "@pure-galaxy/shared",
  "version": "0.1.0",
  "description": "Shared deterministic primitives (PRNG, stable hashing) for PURE GALAXY.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
