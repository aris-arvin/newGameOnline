{
  "name": "pure-galaxy",
  "version": "0.0.0",
  "private": true,
  "description": "PURE GALAXY — browser 4X MMO. Monorepo root.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "serve": "pnpm --filter @pure-galaxy/server run serve",
    "web": "pnpm --filter @pure-galaxy/web run dev",
    "build": "pnpm --filter @pure-galaxy/web run build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "autobattle": "pnpm --filter @pure-galaxy/combat-core run autobattle",
    "meta": "pnpm --filter @pure-galaxy/combat-core run meta"
  }
}
